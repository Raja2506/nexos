version: "3.9"
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: nexos
      POSTGRES_PASSWORD: nexos_pass
      POSTGRES_DB: nexos_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: